import { Navigate } from 'react-router-dom';

// Cette page n'est plus utilisée — la sélection des classes
// est désormais intégrée directement dans GradeManagement.
const PeriodClasses = () => <Navigate to="/notes" replace />;

export default PeriodClasses;
